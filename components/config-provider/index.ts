export { default as ConfigProvider } from './ConfigProvider'
export { useConfig, usePrefixCls, useLocale, CONFIG_PROVIDER_KEY, defaultConfig } from './context'
export type { ConfigProviderProps, ConfigContext, ComponentSize } from './types'
