# v0.46.0

**发布日期**: 2026-08-17

## ✨ 功能增强

### Space

- **新增** `orientation` 属性（推荐使用，优先级高于 `direction`）
- `direction` 标记为兼容旧版，推荐使用 `orientation`
- Props 补齐 `satisfies Record<keyof SpaceProps, any>` 类型约束
- 新增单元测试 67 项

### Image / ImagePreview

- **重构** ImagePreview 组件，提取 `ImagePreviewProps` 接口（内部使用）
- 优化组件结构，补充详细 JSDoc 注释
- Props 补齐 `satisfies Record<keyof ImagePreviewProps, any>` 类型约束
- 修复可选配置的空安全访问（`config?.onTransform`）
- 新增单元测试 92 项

### Tooltip

- **导出** `TooltipOpenChangeInfo`、`TooltipOpenChangeHandler`、`TooltipAfterOpenChangeHandler` 类型

## 🔧 代码优化

### 组件规范化

- **Switch**: props 格式精简，代码结构优化
- **Tag**: props 格式精简
- **Tooltip**: 代码精简，逻辑优化

### 样式优化

- **Segmented**: 删除冗余 CSS 规则（9 行）

### 文档与示例

- 更新 Space 文档与 SpaceSplit 示例
- 更新 Tooltip 受控示例
- 更新 Watermark Portal 示例与文档
- 修正 QRCode CustomSize 示例

### 其他

- 删除内部 CODE_REVIEW.md 文件
- Trigger 组件导入优化

## 📊 测试

- 新增测试 **159 项**（Image 92 + Space 67）
- 全体 **2656 项**单元测试通过

## 📝 类型安全

多个组件补齐 `satisfies Record<keyof XProps, any>` 约束，确保运行时 props 与 TypeScript 接口完全一致，杜绝双源头漂移。

---

**变更文件**: 19 个文件，+495/-382 行
